# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'pimp_test' do
  # self.rubyforge_name = 'pimp_testx' # if different than 'pimp_test'
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=ruby
